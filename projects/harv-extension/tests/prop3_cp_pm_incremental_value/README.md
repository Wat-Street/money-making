# Prop 3 CP+PM Incremental Value

Purpose: test whether adding PM temporal-granularity features improves a CP baseline, especially conditionally.

Current artifact paths:

- `outputs/cp_pm_incremental_tests/`
- `outputs/conditional_pm_cp_tests/`
- `tmp/cp_pm_incremental_tests_outputs/` for local scratch copies, if present.

Main models:

- `CP` or freshly recomputed CP baseline in the Prop 3 script.
- `PM`
- `RAW_PM`
- `RAW_CP_REPO_PLUS_PM` when mapped into repo-CP language.

This is Prop 3. Do not label it as Prop 4.
