# Paper Output Archive

This archive preserves generated outputs that are useful for traceability but are not active inputs to the final manuscript.

- `daily_horizon_checks/spy_probe/` - Small daily-horizon probe output.
- `daily_horizon_checks/test7_scope/` - Daily-horizon checks aligned with the broader `test_7` ticker scope.
- `legacy_builds/paper_tex_build/` - Previous build directory for the alternate paper export.

These folders are preserved rather than deleted.
