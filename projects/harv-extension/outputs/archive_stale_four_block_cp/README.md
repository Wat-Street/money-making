# Archived Stale Four-Block CP Outputs

These outputs used a four-block diagnostic CP benchmark and are not valid as the main CP/PM benchmark.

The correct benchmark for the current PM/CP research package is `CP_REPO_FRESH`, based on the repository `contig_prime_modulo(vol.copy(), n=22, per_day_normalize=False)` construction with repo `RV*` and `CP_*` feature columns.

The archived `cp_ops_hg_tests/` folder may be inspected only as `diagnostic_four_block_projection` material. It must not be used as the primary CP benchmark, paper denominator, or final draft result source.
