# Run Results

This folder contains empirical outputs produced by the modeling pipeline.

## Branches

- `current_intraday/` - Current scratch/output area used by section helpers and benchmark scripts.
- `paper_runs/` - Named empirical runs, including final supporting runs and older historical tests.

Use `paper_runs/README.md` for run-level descriptions.
